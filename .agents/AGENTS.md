# Rules & Guidelines

- **GitHub Synchronization**: Dopo ogni modifica e completamento dei task, effettua sempre il commit e il push dei cambiamenti su GitHub (`git add .`, `git commit`, `git push origin main`).
